---
title: CLI Overview
description: |
  The Spraxium CLI provides seven commands that cover the full development lifecycle.
  Learn when to use each command and how they connect from scaffolding to production.
order: 1
category: CLI
keywords:
  - spraxium cli overview
  - cli overview in spraxium
  - cli cli overview guide
  - cli overview discord bot
  - cli overview typescript
  - spraxium cli documentation
  - cli overview cli
faq:
  - question: "What does \"CLI Overview\" cover in Spraxium?"
    answer: "The Spraxium CLI provides seven commands that cover the full development lifecycle. Learn when to use each command and how they connect from scaffolding to production."
  - question: "When should I use \"CLI Overview\"?"
    answer: "Use this page when you are implementing cli overview in the CLI area of your bot architecture."
  - question: "What should I read after \"CLI Overview\"?"
    answer: "Continue with related CLI pages to connect this concept with practical implementation details."
---

## What the CLI is for

The CLI is the shortest path between a clean project and a runnable bot. It handles scaffolding, file generation, development startup, production builds, runtime launch, database integration, and diagnostics through one consistent interface. That consistency matters because the command line is where teams usually lose time to repeated setup steps, naming drift, or half-manual workflows.

In practice, the CLI is most useful when you let each command do one clear job. _new_ creates the workspace, _generate_ adds framework-aligned files, _dev_ runs the active bot, _build_ validates production output, and _start_ runs the compiled artifact. The remaining commands support adjacent workflows such as database setup and environment diagnostics.

## Available commands

The CLI ships seven commands. Each one targets a distinct phase of the project lifecycle, from scaffolding a brand-new project to running the compiled artifact in production.

<DropdownGroup>
<Dropdown title="spraxium new">
  Creates a new Spraxium project from a template. The interactive wizard asks for a project name, template choice, optional extra packages, and whether to initialize a Git repository. Run it once per project, outside any existing Spraxium workspace.
</Dropdown>

<Dropdown title="spraxium dev">
  Starts the development server using the SWC loader for fast compilation
  cycles. Watches source files and restarts automatically on every save. The
  watcher behavior, entrypoint, and debounce delay are all configurable in
  *spraxium.config.ts*.
</Dropdown>

<Dropdown title="spraxium generate">
  Scaffolds a framework-aligned file from one of nineteen schematics: modules,
  services, tasks, listeners, slash commands, prefix commands, guards, embeds,
  buttons, modals, and more. Auto-registers generated files into the correct
  module array when applicable.
</Dropdown>

<Dropdown title="spraxium build">
  Runs *tsc --noEmit* to type-check, then bundles with *tsdown* for production.
  Returns a non-zero exit code on failure, making it suitable as a CI gate. The
  build output lands in `.spraxium/dist`.
</Dropdown>

<Dropdown title="spraxium start">
  Runs the compiled artifact produced by *spraxium build*. Sets *NODE_ENV* to
  `production` automatically and streams stdio directly to the terminal.
  Requires a completed build before running.
</Dropdown>

<Dropdown title="spraxium database">
  Generates a complete ORM integration module through an interactive wizard.
  Supports Prisma, Drizzle, TypeORM, Mongoose, and MikroORM. Installs
  dependencies and registers the module automatically.
</Dropdown>

<Dropdown title="spraxium info">
  Prints a diagnostic report with the CLI version, Node version, OS, package manager, and all discovered *@spraxium/* package versions. Accepts a *--json* flag for machine-readable output. Useful for bug reports and CI artifacts.
</Dropdown>
</DropdownGroup>

## How the commands fit together

Think about the CLI as a lifecycle, not a bag of unrelated commands:

1. _new_ creates the initial project shape.
2. _generate_ keeps new files aligned with framework conventions.
3. _dev_ gives the fastest feedback loop while implementing.
4. _build_ is the quality gate before sharing or deploying.
5. _start_ runs the exact production artifact you just built.
6. _database_ and _info_ support setup and troubleshooting around that loop.

## Recommended daily flow

Most teams only touch a subset of the CLI every day. The usual rhythm is to scaffold once, generate as needed, stay in _dev_ during implementation, and use _build_ as the final confidence check before shipping.

<Steps>
  <Step title="Scaffold with new">
    Run *spraxium new* once to create the project. Pick your template and
    optional modules during the interactive wizard.
  </Step>
  <Step title="Generate with generate">
    Use *spraxium generate* to add modules, services, listeners, or commands.
    The CLI places files in the right folder and auto-registers them.
  </Step>
  <Step title="Iterate with dev">
    Run *spraxium dev* during implementation. Files reload automatically on save
    so you see results immediately.
  </Step>
  <Step title="Validate with build">
    Run *spraxium build* before every shared environment push. Type errors and
    bundle failures show up here, not in production.
  </Step>
  <Step title="Ship with start">
    Run *spraxium start* in staging and production after a successful build. It
    reads the compiled output directly.
  </Step>
</Steps>

## Local versus CI usage

Local development usually revolves around _generate_ and _dev_. CI usually cares about _build_ and, in some debugging cases, _info --json_. Keeping that split clear helps avoid trying to force interactive commands into automation or relying on development-only behavior in production pipelines.

## Choosing the right command quickly

If you are unsure which command to run, use this shortcut:

1. Need a new project: run _spraxium new_.
2. Need a new framework file: run _spraxium generate_.
3. Need live reload while coding: run _spraxium dev_.
4. Need a deployable artifact: run _spraxium build_.
5. Need to execute the built artifact: run _spraxium start_.
6. Need environment context: run _spraxium info_.

## Next page

Continue with [Creating a Project](/guide/cli-new).
